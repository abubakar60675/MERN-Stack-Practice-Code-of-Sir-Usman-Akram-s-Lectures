console.clear();
// Code
// let address = {
//     title: "Liberty Market",
//     location: "Lahore"

// };

// function printAddress(title, location) {
//     console.log(title + " is located in " + location);
// }
// let { title, location } = address; // This is Object Restructuring.
// printAddress(address.title, address.location);

// console.log(title);

// Code 2
let address = {
    title: "Liberty Market",
    location: "Lahore"

};

function printAddress(title, location) {
    console.log(title + " is located in " + location);
}
let { title, location } = address; // This is Object Restructuring.
printAddress(title, location);

console.log(title);